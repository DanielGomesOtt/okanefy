# 📱 Okanefy – Frontend

This is the **frontend** of the **Okanefy** application — a web interface built with **React + TypeScript**, using **Vite** as the build tool.

This layer is responsible for rendering the user interface, connecting to the backend API, and allowing users to view and manage their financial data and expenses.

---

## 🧰 Technologies

- React  
- TypeScript  
- Vite (build tool)  
- Hero UI (component library)  
- Docker (containerization)

---

## 🚀 Running locally

### Prerequisites
Before getting started, make sure you have:

- Node.js installed
- Docker
- Docker Compose

### Configuration

1. Copy the environment variables file:
```bash
cp .env.example .env
```
